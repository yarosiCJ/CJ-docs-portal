---
sidebar_position: 1
---

import {PortalPublishedArtifacts, PortalWhatsIncluded} from '@site/src/components/PortalWhatsIncluded';

# Clear Junction API docs portal

This repository builds a **static documentation portal** (for Pages deployment):

## What lives where

<PortalWhatsIncluded />

## Local preview

Start the docs portal locally:

```bash
npm run start
```

Build static files:

```bash
npm run build
```

## Where content comes from

The portal publishes:

<PortalPublishedArtifacts />
